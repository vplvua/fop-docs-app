# acts Specification

## Purpose

Act generation — race-safe per-client monthly act numbering, immutable client/contract snapshots, PDF rendering via headless Chromium stored privately in Vercel Blob, plus acts list/detail UI and navigation. Covers FR-ACT-01..10, TC-INTEG-05, TC-INTEG-12.

## Requirements

### Requirement: Race-safe act number generation

The system SHALL generate act numbers under `SELECT ... FOR UPDATE` on acts rows for the same `(client_id, act_date)`. The first act for a client in month M SHALL be numbered `№M`; subsequent acts SHALL be `№M/N` where N is the ordinal position. The UNIQUE index `(client_id, act_date, number)` SHALL serve as a safety net for concurrent inserts.

Covers: FR-ACT-02, FR-ACT-03, TC-INTEG-12.

#### Scenario: First act in month

- **WHEN** no acts exist for client X in April 2026
- **THEN** the act number SHALL be `№4`

#### Scenario: Second act in same month

- **WHEN** one act `№4` already exists for client X in April 2026
- **THEN** the next act number SHALL be `№4/2`

#### Scenario: Concurrent act creation serialized

- **WHEN** two classification attempts create acts for the same client+month simultaneously
- **THEN** the `FOR UPDATE` lock SHALL serialize numbering and both acts SHALL receive distinct numbers

### Requirement: PDF generation via headless Chromium

The system SHALL render act PDFs locally via a React+Tailwind HTML template converted to PDF using headless Chromium (`@sparticuz/chromium` + `puppeteer-core`) in a Vercel Function. The PDF layout SHALL match the format of `samples/acts/act-2026-04_200.pdf`.

Covers: FR-ACT-07, TC-INTEG-05, NFR-PERF-03.

#### Scenario: PDF generated for classified act

- **WHEN** an act with `status = draft` and all snapshot fields populated is passed to the PDF generator
- **THEN** a PDF buffer SHALL be produced containing: ФОП executor header, client details from `client_snapshot`, contract reference from `contract_snapshot`, service table (description, quantity, unit_price, total), signatures block, and `act_date`

#### Scenario: PDF warm render under 2 seconds

- **WHEN** the Chromium instance is warm (reused from a previous render)
- **THEN** PDF generation SHALL complete in under 2 seconds

### Requirement: PDF stored in Vercel Blob (private)

The system SHALL upload generated PDFs to Vercel Blob with private access. The URL SHALL be stored in `act.pdf_file_url`. Download SHALL require a short-lived signed URL generated by the server.

Covers: FR-ACT-08.

#### Scenario: PDF uploaded to Blob

- **WHEN** PDF generation succeeds for act with id `act-123`
- **THEN** the PDF SHALL be uploaded to Blob, and `act.pdf_file_url` SHALL be updated to the Blob URL

#### Scenario: PDF download via signed URL

- **WHEN** the admin clicks "Скачати PDF" on an act card
- **THEN** the system SHALL generate a signed download URL and redirect the browser to download the PDF

### Requirement: PDF regeneration

The system SHALL allow the admin to regenerate a PDF from the act's snapshot data. For `dubidoc` acts, regeneration SHALL be recommended only before `sent_to_edo` status. For `vchasno_external` acts, regeneration SHALL be allowed in any status.

Covers: FR-ACT-09.

#### Scenario: Regenerate PDF for draft act

- **WHEN** the admin clicks "Перегенерувати PDF" on a draft act
- **THEN** a new PDF SHALL be generated from snapshot data, uploaded to Blob, and `pdf_file_url` SHALL be updated

#### Scenario: Regenerate PDF for vchasno_external signed act

- **WHEN** the admin regenerates PDF for an act with `edo_provider = vchasno_external` and `status = signed`
- **THEN** regeneration SHALL succeed (not blocked by status)

### Requirement: Service description editing

The admin SHALL be able to edit `service_description` for acts in `draft` status. For acts with `edo_provider = vchasno_external`, editing SHALL be allowed in any status. After editing, PDF SHALL be auto-regenerated.

Covers: FR-ACT-06.

#### Scenario: Edit service description in draft

- **WHEN** the admin edits `service_description` on a draft act
- **THEN** the field SHALL be updated and the PDF SHALL be regenerated

#### Scenario: Edit blocked for sent_to_edo dubidoc act

- **WHEN** the admin tries to edit `service_description` on a dubidoc act with `status = sent_to_edo`
- **THEN** the edit SHALL be blocked (field read-only in UI)

### Requirement: Auto-trigger PDF after classification

After successful classification creates an act stub, the system SHALL automatically trigger PDF generation. If PDF generation fails, the act SHALL remain in `draft` with `pdf_file_url = NULL` — classification success SHALL NOT be rolled back.

Covers: FR-ACT-01.

#### Scenario: PDF generated after classification

- **WHEN** classification succeeds and creates an act stub
- **THEN** PDF generation SHALL be triggered asynchronously, and `pdf_file_url` SHALL be populated on success

#### Scenario: PDF failure does not block classification

- **WHEN** classification succeeds but Chromium fails to render PDF
- **THEN** the act SHALL exist with `status = draft` and `pdf_file_url = NULL`, and the admin SHALL be able to regenerate later

### Requirement: Acts list page

The system SHALL provide an `/acts` page displaying acts in a table with columns: act_date, number, client name (from snapshot), service_type, status (badge), edo_provider, amount (unit_price × quantity). The list SHALL support filters: status, period (act_date range), client, service_type, edo_provider.

Covers: FR-UI-06.

#### Scenario: View acts list

- **WHEN** the admin navigates to `/acts`
- **THEN** all acts SHALL be displayed sorted by `act_date` descending, then `number`

#### Scenario: Filter by status

- **WHEN** the admin selects status filter "draft"
- **THEN** only acts with `status = draft` SHALL be displayed

### Requirement: Act detail page

The system SHALL provide an `/acts/[id]` page showing: a read-only snapshot panel (client_snapshot, contract_snapshot) labeled "Збережено на момент генерації", editable service_description (when allowed), status badge, PDF download button, PDF regeneration button, and computed total amount.

Covers: FR-UI-07, FR-UI-08.

#### Scenario: View act card with snapshot

- **WHEN** the admin navigates to `/acts/[id]`
- **THEN** the snapshot panel SHALL display client name, legal_id, address, bank details, contract number, and signed_date — all from snapshot fields, not live client data

#### Scenario: Download PDF

- **WHEN** the admin clicks "Скачати PDF" and `pdf_file_url` is set
- **THEN** the browser SHALL download the PDF file

#### Scenario: Download PDF not available

- **WHEN** the admin views an act with `pdf_file_url = NULL`
- **THEN** the download button SHALL be disabled with hint "PDF ще не згенеровано"

### Requirement: Navigation includes Acts

The top-bar navigation SHALL include an "Акти" link pointing to `/acts`, positioned between "Платежі" and "Налаштування".

#### Scenario: Acts link visible

- **WHEN** the admin is logged in
- **THEN** the top-bar SHALL show "Акти" link
